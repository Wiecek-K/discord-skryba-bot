# --- Konfiguracja dla PyTorch (CPU-only) ---
# Mówimy pip, aby oprócz standardowego repozytorium (PyPI),
# sprawdzał również to specjalne repozytorium dla wersji CPU PyTorch.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Główne zależności bota ---
torch
torchvision
torchaudio
discord.py
python-dotenv
requests
PyGithub
chromadb-client
sentence-transformers
